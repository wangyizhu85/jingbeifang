import java.math.BigDecimal;

public class FullDiscount{
    BigDecimal full;
    BigDecimal discount;
    FullDiscount(BigDecimal full,BigDecimal discount){
        this.full = full;
        this.discount = discount;
    }
}